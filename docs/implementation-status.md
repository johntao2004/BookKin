# BookKin V1 实施状态

更新日期：2026-09-06

## 已落地

- 规范：设计系统、技术架构、仓库协作规则、3 项 ADR、OpenAPI 契约、运维/备份手册。
- 前端：React 19、Ant Design 6、Ant Design Icons、AntV G2 基础入口、响应式藏书馆、用户管理、NAS 状态、文件任务、回收站、当前元数据直接覆盖、EPUB 阅读器、PDF 文字重排/图片页/原版三类呈现、书签/划线/笔记。
- 后端：Java 21、Spring Boot 4.1 模块化单体、Session/CSRF/Argon2id/限流/审计、游标目录、扫描与封面提取、Range 阅读、按书聚合的私人批注及 DOCX/XLSX 导出。
- 文件事务：服务端预览、指纹前置条件、幂等键、入队状态原子提交、读写租约、同根移动、跨根复制校验、回收/恢复，以及带事务内临时回滚的元数据直接覆盖。
- NAS：多根目录环境配置、每分钟能力检查、只读/离线降级告警、Compose override 示例、30 天保留任务。
- 书籍上传：EPUB/PDF 流式暂存、队列恢复、本地识别、Open Library/Google Books 候选、字段来源、封面裁切、重复/相似检测、人工校对和幂等入库。
- 交付：`app`/`worker`/`postgres` Compose、多阶段镜像、ARM64/AMD64 Buildx 工作流、备份恢复脚本、十万册数据与 p95 压测脚本。
- 本机数据环境：原生 PostgreSQL 18.6、Flyway V1-V7、JDBC Session、4 本使用不同正式内嵌封面的 EPUB 演示书；书库与用户从数据库读取，默认阅读进度、阅读时长和批注均为空。Flyway 初始化本身不预置书籍。

## 本次验证（2026-09-06）

- ESLint、TypeScript：通过。
- Vitest：`pnpm test --maxWorkers=1` 全套 48 个文件、188/188 通过（497.76 秒）。默认双工作进程首跑 183/188，5 个异步页面等待失败；不修改断言的单工作进程复跑全部通过，首轮失败保留为并发运行稳定性限制。
- Vite：`pnpm build` 通过，Sites 产物准备完成。
- Maven verify：34 个测试通过，0 失败、0 错误、0 跳过，可执行 JAR 构建成功。
- Token 漂移检查与 Sites 路由测试：通过，Sites 4/4。
- 浏览器：本轮复核字体上传对齐、藏书页加载与 NAS 重新检查；既有控制台仍有 Ant Design Drawer `width` 弃用警告。
- 历次虚拟书库桌面交互及截图见 [QA 记录](../design/qa/gothic-library/README.md)。本轮不宣称生产部署、远端 CI 或真实 NAS 故障演练完成。

## 本次功能与文档更新

- 统一按钮图标/文字槽、中文标签、菜单关闭行为、主题下拉、导航与内容边缘。
- 藏书概览骨架与统一高度、封面四角裁切；NAS 卡片紧凑高度；用户管理采用数据表。
- 字体管理新增阿里巴巴普惠体，整理思源黑体显示名称；上传控件对齐，去掉列表尾部冗余说明。预设字体开关不可停用，自定义字体可切换状态。
- 原创哥特式虚拟书库：净空拱顶、连续吊灯悬挂、无中央桌椅、天球查询和炉内体积火焰，保留真实书籍与独立房间交互。
- 性能脚本统一迁入 `scripts/benchmark/`，文档与截图集中在 `docs/` 和 `design/qa/`。

## 历史环境验收（2026-09-02，本轮未重复全部场景）

| 检查 | 结果 |
|---|---|
| TypeScript 类型检查 | 通过 |
| ESLint（0 warning） | 通过 |
| Vitest 交互测试 | 44 个测试文件、177/177 通过；包含真实模式登录凭据边界、PDF 文本重排与扫描页空内容边界，以及书库概览卡片零数据、周同比与统计图底部留白状态 |
| Vite 生产构建 | 通过 |
| Sites 静态路由测试 | 4/4 通过 |
| Java/JUnit | 34/34 通过、0 跳过；Testcontainers 使用 PostgreSQL 18.6 验证 Flyway、目录组织、字体暂存清理，以及同步内容流的 Range/HEAD/416 语义 |
| Maven 可执行 JAR | 通过 |
| 前端依赖与构建开销 | 已移除旧 UI 组件库与直接 Emotion 依赖，改用 Ant Design 6、Ant Design Icons；AntV G2 作为统一图表入口接入；Vite 使用 Ant Design 按需入口 |
| 原生 PostgreSQL 18.6 / Flyway V1-V7 | 通过；API 与 Worker 使用不可变 JAR 快照启动，健康检查与 readiness 均为 `UP` |
| 数据库演示状态 | 3 用户、4 本逻辑书、4 个可用文件、0 个回收站条目；4 本书分别使用 4 张不同的正式封面，默认阅读进度、阅读时长、书签、划线和笔记均为 0 |
| 默认演示目录 | 隔离生成 4 本 EPUB、4/4 均含不同的正式内嵌封面，不再生成重复封面或白底 PDF 首页占位书；Flyway 初始化仍保持空书库 |
| 扫描与重复上传 | 4 个真实 EPUB 扫描完成；流式暂存、SHA-256 和重复文件阻止通过；PDF 能力由上传、内容流和自动化测试覆盖 |
| 阅读与权限 | OWNER/MEMBER 阅读进度按用户隔离并持久化；成员访问用户管理返回 403 |
| EPUB/PDF 内容流 | 完整响应、Range 206、HEAD 无正文、越界 416（含 `Content-Range: bytes */size`）和租约释放通过；400 次、24 并发 Range 请求全部成功且服务日志无错误 |
| 浏览器真实模式 | 公开首页和登录页通过；登录页不预填或展示虚构数据库密码，控制台无 error/warning；已初始化实例隐藏初始化入口 |
| Token 漂移检查 | 通过 |
| Compose 与容器运行 | 配置解析通过；本机 ARM64 镜像构建通过，隔离环境中 `app`/`worker`/`postgres` 三容器启动，PostgreSQL 与 app 健康、readiness `UP`、静态首页 200、初始化状态 API 200、容器内 Flyway V7，日志无应用异常；测试容器、网络和卷已清理 |
| 备份/恢复脚本语法 | 通过 |

## 仍需在生产容器与真实 NAS 环境完成

这些是运行环境验收，不是以静态代码检查替代的项目：

1. 在 CI/AMD64 主机完成 AMD64 镜像实际启动验收；本机 ARM64 三容器链路已经通过。
2. 注入 10 万册/15 万文件数据，实测列表 p95 ≤ 250ms、搜索 p95 ≤ 500ms。
3. 使用代表性 EPUB/PDF 在真实 NAS 上执行断连、空间不足、复制中断、校验失败、权限丢失、大小写冲突和进程中断演练。
4. 对加密/DRM/签名/损坏文件及异常 ZIP 做真实样本回归，并完成备份恢复演练。

本次生产构建报告大分块警告：Three.js 582.05 kB（gzip 147.49 kB），共享 UI 785.24 kB（gzip 252.27 kB）。未调高阈值隐藏告警。测试环境另有 jsdom CSS 解析与伪元素 getComputedStyle 提示。

## Figma 边界

Figma Draft 已同步 Foundations（5 个变量集合、73 个变量、9 个文本样式、3 个效果样式）。Starter MCP 调用额度在组件与页面阶段触顶，因此完整页面尚未同步；Figma 范围见 [`design/figma-v1-scope.md`](../design/figma-v1-scope.md)，本地视觉记录集中在 `design/qa/`。远端 Figma 本轮未重新同步。浏览器交互结论必须以当前分支的现场验证为准。
