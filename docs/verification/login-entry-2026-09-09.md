# 登录入口白屏排查

- 截图地址为 `http://127.0.0.1:4173/login/`。实测返回 HTTP 500、`X-Powered-By: Next.js`；监听进程工作目录为另一个项目 `/Users/johntao/Documents/ChatGPT/New project`。
- BookKin 的现有预览运行在 4175。未停止或修改其他项目。
- BookKin 登录页现在等待会话检查；已有有效会话跳转首页，临时密码会话跳转首次改密，匿名会话显示登录表单。会话读取失败提供明确提示与重试。
- 浏览器真实访问 `http://127.0.0.1:4175/login/`，自动跳转 `/library`，首页正常显示。
- 新增普通会话、临时密码会话访问带尾斜线登录地址的回归测试；保留匿名登录测试。
