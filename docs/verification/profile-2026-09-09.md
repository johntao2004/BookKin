# 个人信息页面

- 新增登录保护的 `/profile`，展示当前账户的显示名称、用户名和角色（只读）。
- 找回邮箱绑定表单和状态整合为个人信息分区，沿用现有验证接口。
- 账户菜单新增个人信息；移除设置里的找回邮箱标签，旧 `/settings#recovery-email` 自动跳转。普通成员的设置入口归入个人信息。
- 提供修改密码入口，普通改密文案与临时密码流程分开，普通改密完成后返回个人信息。
- 浏览器确认旧链接跳转、账户菜单、三个资料分区、修改密码入口及无横向溢出；未提交密码或邮件表单。
- Lint、类型检查通过；SettingsPage/AppShell 共 10 项定向测试通过。

## 资料编辑与页内改密

- 用户名、账户名字现可编辑；保存时验证当前密码、用户名格式与唯一性。角色仍只读。
- `/auth/profile` 只操作当前身份，事务内更新资料并审计；改名后撤销旧用户名会话，当前请求重新建立认证会话。
- 密码表单嵌入个人信息页，沿用现有复杂度规则，与资料和邮箱独立保存。
- 前端 lint/typecheck/build 通过，新增独立保存测试通过。
- 后端 verify：88 项，0 失败，6 项因 Docker 不可用跳过；随后新增 ProfileService 的 4 项测试全部通过。
- 本地使用 start:local 不可变 JAR 快照，API/Worker 健康。修正 ProfileService 仅在 API profile 加载。
- 临时 MEMBER 实际 API 验证通过：错误密码、重复用户名、改名及显示名更新、刷新当前会话、撤销旧会话、新用户名登录、修改密码、新密码登录、旧用户名拒绝。测试账户和关联会话/审计已清理。
- 浏览器确认个人信息页显示三个独立可编辑表单；用户账户未修改。
